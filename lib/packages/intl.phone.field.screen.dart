import 'package:packages_demo/libs.dart';

class IntlPhoneFieldScreen extends StatelessWidget {
  const IntlPhoneFieldScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: appBar(titleText: 'Intl Phone Field'),
      ),
    );
  }
}
